# ─────────────────────────────────────────────────────────────────────────────
# GPSS API 驗證碼
#
# 申請：全球專利檢索系統 → API → 使用說明 → 驗證碼申請 → API 驗證碼線上申請
#       一個 EMAIL 只能申請一組，審核通過後次日寄送
# 效期：365 日。到期前一個月或已過期時可用「驗證碼展期」延長
#       - 到期前展期 → 到期日 + 365 日
#       - 到期後展期 → 展期當日 + 365 日
#
# ⚠ 這個檔案絕對不能進 git。確認 .gitignore 有 .env
# ⚠ 驗證碼過期後若被重試邏輯連續打，IP 會被封鎖（API 文件「IP blocked」）
# ─────────────────────────────────────────────────────────────────────────────

GPSS_USER_CODE=在這裡貼上你的驗證碼
